name: Deploy on Release

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tt: ${{ secrets.JASMIN_CLIENT_SECRET }}
    steps:
      # Checkout repository
      - uses: actions/checkout@v2
      # Determine release name (e.g. v1.0.0, master)
      - name: tt
        run: echo ${{ secrets.T1 }}
