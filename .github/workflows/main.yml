name: Deploy on Release

on:
  push:
  workflow_dispatch:

env:
  TT: ${{ secrets.*.name }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # Checkout repository
      - uses: actions/checkout@v2
      # Determine release name (e.g. v1.0.0, master)
      - name: tt
        run: |
          echo $TT 
          #for secret in $(echo ${{ secrets.*.name }})
          #do
          #  print $secret
          #done
